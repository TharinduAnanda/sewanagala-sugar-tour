import nodemailer from 'nodemailer'

// Email configuration
const EMAIL_CONFIG = {
  host: 'smtp.gmail.com',
  port: 587,
  secure: false,
  auth: {
    user: process.env.EMAIL_USER || 'tharindulalanath49@gmail.com',
    pass: process.env.EMAIL_APP_PASSWORD || 'pekn uqhy kklc ltjh'
  }
}

// Create transporter
const transporter = nodemailer.createTransport(EMAIL_CONFIG)

interface BookingEmailData {
  booking_id: string
  name: string
  email: string
  phone: string
  visit_date: string
  visit_time: string
  visitor_count: number
  booking_date: string
}

// Generate HTML email template
const generateEmailHTML = (bookingData: BookingEmailData): string => {
  const { booking_id, name, visit_date, visit_time, visitor_count } = bookingData

  return `
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Confirmation</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            background-color: #f4f4f4;
        }
        .container {
            background-color: #ffffff;
            margin: 20px;
            padding: 0;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            overflow: hidden;
        }
        .header {
            background: linear-gradient(135deg, #2c5f2d 0%, #97bc62 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: bold;
        }
        .content {
            padding: 30px;
        }
        .booking-ref {
            background-color: #f8f9fa;
            border-left: 4px solid #2c5f2d;
            padding: 15px;
            margin-bottom: 25px;
        }
        .booking-ref h2 {
            margin: 0 0 10px 0;
            color: #2c5f2d;
            font-size: 20px;
        }
        .booking-ref .ref-number {
            font-size: 24px;
            font-weight: bold;
            color: #2c5f2d;
            font-family: 'Courier New', monospace;
        }
        .details-table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px 0;
        }
        .details-table td {
            padding: 12px;
            border-bottom: 1px solid #e0e0e0;
        }
        .details-table td:first-child {
            font-weight: bold;
            color: #2c5f2d;
            width: 40%;
        }
        .instructions {
            background-color: #e8f5e9;
            border-radius: 8px;
            padding: 20px;
            margin: 20px 0;
        }
        .instructions h3 {
            color: #2c5f2d;
            margin-top: 0;
        }
        .instructions ul {
            margin: 10px 0;
            padding-left: 20px;
        }
        .instructions li {
            margin: 8px 0;
        }
        .footer {
            background-color: #f8f9fa;
            padding: 20px;
            text-align: center;
            font-size: 12px;
            color: #666;
        }
        .button {
            display: inline-block;
            padding: 12px 30px;
            background-color: #2c5f2d;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            margin: 10px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>SEWANAGALA SUGAR FACTORY</h1>
            <p>Tour Booking Confirmation</p>
        </div>
        
        <div class="content">
            <p>Dear ${name},</p>
            <p>Thank you for booking a tour with us! Your booking has been confirmed.</p>
            
            <div class="booking-ref">
                <h2>Booking Reference</h2>
                <div class="ref-number">${booking_id}</div>
                <p style="margin: 5px 0 0 0; font-size: 12px; color: #666;">Please keep this reference number for your records</p>
            </div>
            
            <h3 style="color: #2c5f2d;">Tour Details</h3>
            <table class="details-table">
                <tr>
                    <td>Date</td>
                    <td>${new Date(visit_date).toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' })}</td>
                </tr>
                <tr>
                    <td>Time</td>
                    <td>${visit_time}</td>
                </tr>
                <tr>
                    <td>Number of Visitors</td>
                    <td>${visitor_count}</td>
                </tr>
                <tr>
                    <td>Status</td>
                    <td><strong style="color: #2c5f2d;">CONFIRMED</strong></td>
                </tr>
            </table>
            
            <div class="instructions">
                <h3>Important Instructions</h3>
                <ul>
                    <li>Please arrive <strong>15 minutes before</strong> your scheduled time</li>
                    <li>Bring a valid <strong>ID for check-in</strong></li>
                    <li>Wear <strong>comfortable walking shoes</strong></li>
                    <li>Photography is allowed in most areas</li>
                    <li>The tour duration is approximately <strong>2 hours</strong></li>
                </ul>
            </div>
            
            <div style="text-align: center; margin: 30px 0;">
                <p style="color: #666;">Need to make changes?</p>
                <p style="font-size: 14px;">
                    Phone: <strong>+94 77 123 4567</strong><br>
                    Email: <strong>tours@sewanagalafactory.com</strong>
                </p>
            </div>
        </div>
        
        <div class="footer">
            <p>Thank you for choosing Sewanagala Sugar Factory Tours!</p>
            <p>This is an automated email. Please do not reply to this message.</p>
        </div>
    </div>
</body>
</html>
  `
}

// Send booking confirmation email
export async function sendBookingConfirmationEmail(bookingData: BookingEmailData): Promise<{ success: boolean; message: string; error?: string }> {
  try {
    const htmlContent = generateEmailHTML(bookingData)

    const mailOptions = {
      from: {
        name: 'Sewanagala Sugar Factory Tours',
        address: EMAIL_CONFIG.auth.user
      },
      to: bookingData.email,
      subject: `Tour Booking Confirmation - ${bookingData.booking_id}`,
      html: htmlContent
    }

    await transporter.sendMail(mailOptions)

    console.log(`Email sent successfully to ${bookingData.email}`)

    return {
      success: true,
      message: 'Email sent successfully'
    }
  } catch (error: any) {
    console.error('Email sending failed:', error.message)
    return {
      success: false,
      message: 'Failed to send email',
      error: error.message
    }
  }
}

// Test email connection
export async function testEmailConnection(): Promise<{ success: boolean; message: string }> {
  try {
    await transporter.verify()
    console.log('Email server connection verified')
    return { success: true, message: 'Email server is ready' }
  } catch (error: any) {
    console.error('Email server connection failed:', error.message)
    return { success: false, message: 'Email server connection failed' }
  }
}
